### AL ###
#Template for AL projects for Dynamics 365 Business Central
#launch.json folder
.vscode/
#Cache folder
.alcache/
#Symbols folder
.alpackages/
#Snapshots folder
.snapshots/
#Testing Output folder
.output/
# Local .terraform directories
.terraform/

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version 
# control as they are data points which are potentially sensitive and subject 
# to change depending on the environment.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore transient lock info files created by terraform apply
.terraform.tfstate.lock.info
.terraform.lock.hcl
terraform.tfstate.backup
# Include override files you do wish to add to version control using negated pattern
# !example_override.tf
.terraform.lock.hcl
# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
terraform.tfstate
# example: *tfplan*
plan.tfplan
# Ignore CLI configuration files
.terraformrc
terraform.rc
#Extension App-file
*.app
#Rapid Application Development File
rad.json
#Translation Base-file
*.g.xlf
#License-file
*.flf
#Test results file
TestResults.xml
*.env
# ---- Ansible ----
inventory.ini      # generated dynamically, not stored in repo
*.retry            # ansible retry files
host_vars/         # per-host secrets (optional)
group_vars/*.yml   # group vars (may contain secrets, keep ignored unless sanitized)

# ---- Keys & Certificates ----
*.pem
*.key
*.crt
*.pub
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub

# ---- Logs & Outputs ----
*.log
*.out
*.bak
*.swp
*.tmp

# ---- Python / Helper scripts ----
__pycache__/
*.pyc

# ---- System / Editor files ----
.DS_Store
Thumbs.db
.vscode/
.idea/
*.code-workspace
terraform.tfvars
oci_api_key.pem
oci_api_key.pub
terraform.tfstate.backup
ssh_config
oci_api_key.pem
oci_api_key.pub
selfsigned.key
selfsigned.crt
vault.yml